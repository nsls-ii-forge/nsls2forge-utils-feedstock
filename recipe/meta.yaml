{% set name = "nsls2forge-utils" %}
{% set version = "0.0.1" %}
{% set sha256 = "604d6152b4b80c9a1e598cadb10b6e722277a5b6f5a1330c18f52c48c9c17f03" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  entry_points:
    - check-results = nsls2forge_utils.cli:main
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - nsls2forge_utils
  commands:
    - check-results --help

about:
  home: https://github.com/nsls-ii-forge/nsls2forge-utils
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: nsls2forge utilities for packaging
